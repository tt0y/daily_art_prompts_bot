# Drawing Bot 🎨

Telegram бот для художников, который присылает случайные слова-ассоциации для рисования каждый день. Помогает бороться с арт-блоком и находить новые идеи.

## Возможности

- 📅 **Ежедневная рассылка**: Бот автоматически присылает случайное слово в заданное время.
- 🎲 **Случайное слово**: Команда `/word` позволяет получить тему для рисования в любой момент.
- 👥 **Поддержка групп**: Работает как в личных сообщениях, так и в групповых чатах.

## Команды

- `/start` - Начать работу (подписаться на рассылку)
- `/help` - Справка по боту
- `/word` - Получить случайное слово прямо сейчас
- `/about` - Информация о боте и настройках

## Установка и запуск

### Предварительные требования

- Python 3.9+
- Docker и Docker Compose (для запуска в контейнере)

### Запуск на VDS (Ubuntu/Debian)

**Python на сервере устанавливать НЕ НУЖНО.** Все работает внутри Docker.

1. **Зайдите на сервер и клонируйте репозиторий:**
   ```bash
   git clone git@github.com:tt0y/daily_art_prompts_bot.git
   cd daily_art_prompts_bot
   ```

2. **Запустите скрипт установки:**
   Скрипт сам установит Docker, если его нет, и запустит бота.
   ```bash
   chmod +x deploy.sh
   ./deploy.sh
   ```

3. **Настройте токен:**
   Скрипт создаст файл `.env` и попросит вас его отредактировать.
   ```bash
   nano .env
   # Вставьте токен и сохраните (Ctrl+O, Enter, Ctrl+X)
   ```

4. **Перезапустите бота (после настройки .env):**
   ```bash
   ./deploy.sh
   ```

### Локальный запуск (для разработки)

1. **Клонируйте репозиторий:**
   ```bash
   git clone git@github.com:tt0y/daily_art_prompts_bot.git
   cd daily_art_prompts_bot
   ```

2. **Создайте виртуальное окружение:**
   ```bash
   python -m venv .venv
   source .venv/bin/activate  # Linux/macOS
   # .venv\Scripts\activate  # Windows
   ```

3. **Установите зависимости:**
   ```bash
   pip install -r requirements.txt
   ```

4. **Настройте переменные окружения:**
   Создайте файл `.env` на основе `.env.example`:
   ```bash
   cp .env.example .env
   ```
   Отредактируйте `.env` и укажите ваш токен бота и другие настройки.

5. **Запустите бота:**
   ```bash
   python main.py
   ```

## Структура проекта

- `main.py` - Точка входа, настройка бота и планировщика.
- `handlers/` - Обработчики команд Telegram.
- `services/` - Бизнес-логика (работа с БД, выбор слов).
- `data/` - Данные (база данных SQLite, список слов JSON).
- `config.py` - Конфигурация проекта.

## Лицензия

MIT
